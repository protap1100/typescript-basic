// Reference  Type --Object;

const user: {
  firstName: string;
  middleName?: string; // ? Means optional type just like optional chaining
  lastName: string;
  age: number;
  isEmployed: boolean;
} = {
  firstName: "Protap",
  lastName: "Biswas",
  age: 23,
  isEmployed: false,
};
